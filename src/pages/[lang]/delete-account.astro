---
// Every non-default locale: /<lang>/delete-account
import DeleteAccountPage from '../../components/DeleteAccountPage.astro';
import { nonDefaultLocalePaths } from '../../i18n/paths';
import type { Locale } from '../../i18n/utils';

export const getStaticPaths = nonDefaultLocalePaths;

const { locale } = Astro.props as { locale: Locale };
---

<DeleteAccountPage locale={locale} />
